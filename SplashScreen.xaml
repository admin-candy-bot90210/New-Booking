<Window x:Class="DJBookingSystem.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading..." Height="500" Width="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <!-- Fade animation -->
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1.0" To="0.0" Duration="0:0:1"/>
        </Storyboard>
    </Window.Resources>

    <Border Background="#1a1a1a" CornerRadius="15" BorderBrush="#E67E22" BorderThickness="3">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <!-- Logo Image -->
            <Viewbox Grid.Row="0" Margin="50,30,50,30">
                <Image x:Name="LogoImage"
                       Source="Assets/SplashLogo.png"
                       Stretch="Uniform"/>
            </Viewbox>

            <!-- Loading Text -->
            <TextBlock x:Name="LoadingTextBlock"
                      Grid.Row="1"
                      Text="LOADING..."
                      FontSize="28"
                      FontWeight="Bold"
                      Foreground="White"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>

            <!-- Progress Bar Section -->
            <Grid Grid.Row="2" Margin="50,0,50,30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>

                <!-- Percentage Text -->
                <TextBlock x:Name="PercentageTextBlock"
                          Grid.Row="0"
                          Text="0%"
                          FontSize="24"
                          FontWeight="Bold"
                          Foreground="White"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>

                <!-- Progress Bar Container -->
                <Border Grid.Row="1"
                       Background="#2C2C2C"
                       CornerRadius="15"
                       BorderBrush="#444"
                       BorderThickness="2">
                    <Grid>
                        <!-- Multicolored Progress Bar -->
                        <Border x:Name="ProgressBarFill"
                               HorizontalAlignment="Left"
                               CornerRadius="13"
                               Width="0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#FF6B6B" Offset="0.0"/>
                                    <GradientStop Color="#FFA500" Offset="0.2"/>
                                    <GradientStop Color="#FFD700" Offset="0.4"/>
                                    <GradientStop Color="#90EE90" Offset="0.6"/>
                                    <GradientStop Color="#4169E1" Offset="0.8"/>
                                    <GradientStop Color="#9370DB" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
