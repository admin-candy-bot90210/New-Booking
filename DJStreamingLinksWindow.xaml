<Window x:Class="DJBookingSystem.DJStreamingLinksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DJ Streaming Links" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#ECF0F1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <StackPanel>
                <TextBlock Text="🎧 DJ Streaming Links Directory"
                          FontSize="20"
                          FontWeight="Bold"
                          Foreground="White"
                          Margin="0,0,0,5"/>
                <TextBlock Text="Quick access to all DJ streaming links for bookings and events"
                          FontSize="12"
                          Foreground="#ECF0F1"/>
            </StackPanel>
        </Border>

        <!-- DJ List -->
        <Border Grid.Row="1" Margin="10" Background="White" BorderBrush="#BDC3C7" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" Background="#F8F9FA" Padding="10">
                    <Button Content="🔄 Refresh"
                           Click="Refresh_Click"
                           Padding="10,5"
                           Margin="0,0,10,0"
                           Background="#3498DB"
                           Foreground="White"
                           BorderThickness="0"
                           Cursor="Hand"/>
                    <TextBlock Text="Total DJs:"
                              FontWeight="Bold"
                              VerticalAlignment="Center"
                              Margin="15,0,5,0"/>
                    <TextBlock x:Name="TotalDJsTextBlock"
                              Text="0"
                              VerticalAlignment="Center"
                              Foreground="#3498DB"
                              FontWeight="Bold"/>
                </StackPanel>

                <!-- DataGrid -->
                <DataGrid x:Name="DJLinksDataGrid"
                         Grid.Row="1"
                         Margin="10"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         SelectionMode="Single"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         GridLinesVisibility="Horizontal"
                         AlternatingRowBackground="#F8F9FA"
                         RowBackground="White"
                         HeadersVisibility="Column"
                         RowHeight="45">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="DJ Name" Binding="{Binding Username}" Width="180" FontWeight="Bold">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#2C3E50"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="5"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Streaming Link" Binding="{Binding StreamingLink}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#3498DB"/>
                                    <Setter Property="TextDecorations" Value="Underline"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="5"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Member Since" Binding="{Binding CreatedAt, StringFormat='{}{0:MMM dd, yyyy}'}" Width="130">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="5"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Actions" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Button Content="🔗 Copy Link"
                                               Click="CopyLink_Click"
                                               Tag="{Binding StreamingLink}"
                                               Padding="8,4"
                                               Margin="2"
                                               Background="#27AE60"
                                               Foreground="White"
                                               BorderThickness="0"
                                               Cursor="Hand"
                                               FontSize="11"/>
                                        <Button Content="🌐 Open in Browser"
                                               Click="OpenInBrowser_Click"
                                               Tag="{Binding StreamingLink}"
                                               Padding="8,4"
                                               Margin="2"
                                               Background="#3498DB"
                                               Foreground="White"
                                               BorderThickness="0"
                                               Cursor="Hand"
                                               FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
